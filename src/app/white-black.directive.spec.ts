import { WhiteBlackDirective } from './white-black.directive';

describe('WhiteBlackDirective', () => {
  it('should create an instance', () => {
    const directive = new WhiteBlackDirective();
    expect(directive).toBeTruthy();
  });
});
